query Task($taskId: ID!) {
  task(taskId: $taskId) {
    metadata {
      name
      description
      owner
      source
    }
    status {
      state
      retriesLeft
    }
    retries
    created
    deadline
    expires
    priority
    provisionerId
    workerType
    schedulerId
    dependencies
  }
}
