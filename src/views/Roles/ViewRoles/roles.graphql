query RoleIds($clientsConnection: PageConnection, $filter: JSON) {
  listRoleIds(connection: $clientsConnection, filter: $filter) {
    pageInfo {
      hasNextPage
      hasPreviousPage
      cursor
      previousCursor
      nextCursor
    }
    edges {
      node {
        roleId
      }
    }
  }
}